import Sidebar from '../../../../../src/components/sidebar/index.vue';

describe('Sidebar - Index.vue', () => {
  it('should set the component name', () => {
    expect(Sidebar.name).to.equal('sidebar');
  });
});
